# configuration/storage.yaml
#
# Written by `scribe forge` from the storage 1.0.0
# declaration. Values are the package defaults, and this file is yours to
# edit: `scribe forge` never overwrites it.

# The largest object the API accepts, in bytes, refused at the door above it.
max_object_bytes: 104857600

# Whether an image may be asked for at another size, which starts imgproxy.
image_transformation: true

# Where each resource this package needs is placed, per target. A target
# that is not named here gets a container alongside the rest of the stack.
#
# The resources to name here are: objects.
deploy: {}
